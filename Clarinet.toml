[project]
name = 'Decentralized-Education-Credential-Verification-System'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.credential-registry_]
path = 'contracts/credential-registry_.clar'
clarity_version = 2
epoch = 2.5

[contracts.institution-management]
path = 'contracts/institution-management.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
